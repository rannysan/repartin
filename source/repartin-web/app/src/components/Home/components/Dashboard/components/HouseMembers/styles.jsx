import { createStyles } from "@material-ui/core/";

export default ( theme ) => {

  return createStyles( {
    dialogPaper: {
      margin: "0"
    },
    dialogStyle: {
      padding: "0"
    },
    listItem: {
      width: "338px"
    },
    icon: {
      fontSize: "1.14em"
    }
  } );
}